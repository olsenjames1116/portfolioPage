import Navbar from '../Navbar/Navbar';
import Hero from '../Hero/Hero';

// Represents the Header component at the top of the page.
const Header = () => {
  return (
    <header>
      <Navbar />
      <Hero />
    </header>
  );
};

export default Header;
